# Copie este arquivo para .env.local e troque os valores.
# Login e senha padrão do painel administrativo (/admin).
# Se não configurar nada, o site usa admin / admin123 por padrão —
# troque isso antes de publicar o site de verdade!
ADMIN_USER=admin
ADMIN_PASSWORD=admin123
